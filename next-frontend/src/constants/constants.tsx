const sharedClasses = {
    card: 'bg-card p-4 rounded-lg shadow-md',
    primaryColor: 'text-primary',
    primaryColorHover: "hover:text-green-600",
    secondaryColor: 'bg-secondary text-secondary-foreground px-3 py-1 rounded',
    mutedForeground: 'text-muted-foreground',
    text: 'text-white'

  }
export default sharedClasses
const inputClasses = 'w-full px-4 py-3 rounded-lg border border-accent focus:outline-none focus:ring focus:ring-primary transition duration-200 hover:border-accent/80'
const labelClasses = 'block text-sm font-medium text-muted'
const buttonClasses = 'px-4 py-2 rounded-lg bg-primary text-white font-medium hover:bg-primary/80 transition duration-200'
const linkClasses = 'text-primary hover:text-primary/80 transition duration-200'
const errorClasses = 'text-red-500 text-sm font-medium'
const successClasses = 'text-green-500 text-sm font-medium'